
window.onload = function(){  
    // const tower = document.getElementById("tokyo-tower")
    // const kimono = document.getElementById("tokyo-kimono")
    // const streetFood = document.getElementById("tokyo-street-food")


    // tower.classList.add("scale")

    // const arrayCard = [tower,kimono,streetFood]

    // for (let index = 0; index < arrayCard.length; index++) {
    //     const element = arrayCard[index];
    //     console.log(element)

    //     element.classList.add("scale")
    //     element.classList.remove("scale")  

    //     console.log(element.firstChild)
    // }


 
    // streetFood.classList.add("none")

    // kimono.addEventListener("click", function(){
    //     tower.classList.remove("scale")
    //     streetFood.classList.remove("scale")

    //     tower.classList.add("min-scale")
    //     streetFood.classList.add("min-scale")

    //     tower.classList.add("opacity")
    //     streetFood.classList.add("opacity")

    //     kimono.classList.remove("min-scale")
    //     kimono.classList.remove("opacity")
    //     kimono.classList.add("scale")

    // })
    // streetFood.addEventListener("click", function(){
    //     tower.classList.remove("scale")
    //     kimono.classList.remove("scale")

    //     tower.classList.add("opacity")
    //     kimono.classList.add("opacity")

    //     tower.classList.add("min-scale")
    //     kimono.classList.add("min-scale")
        
    //     streetFood.classList.remove("min-scale")
    //     streetFood.classList.remove("opacity")
    //     streetFood.classList.add("scale")
    // })
    // tower.addEventListener("click", function(){
    //     kimono.classList.remove("scale")
    //     streetFood.classList.remove("scale")

    //     kimono.classList.add("min-scale")
    //     streetFood.classList.add("min-scale")

    //     kimono.classList.add("opacity")
    //     streetFood.classList.add("opacity")

    //     tower.classList.remove("min-scale")
    //     tower.classList.remove("opacity")
    //     tower.classList.add("scale")

    // })
    // const card = document.getElementsByClassName("card")
    // const mainBackground = document.getElementById("main")

    // console.log(mainBackground)
    // console.log(card)
    
//     const state = {};
// const carouselList = document.querySelector('.carousel__list');
// const carouselItems = document.querySelectorAll('.carousel__item');
// const elems = Array.from(carouselItems);

// carouselItems.addEventListener('click', function (event) {
//   var newActive = event.target;
//   var isItem = newActive.closest('.carousel__item');
//     console.log(newActive, isItem)
//   if (!isItem || newActive.classList.contains('carousel__item_active')) {
//     return;
//   };
   
//   update(newActive);
// });

// const update = function(newActive) {
//   const newActivePos = newActive.dataset.pos;

//   const current = elems.find((elem) => elem.dataset.pos == 0);
//   const prev = elems.find((elem) => elem.dataset.pos == -1);
//   const next = elems.find((elem) => elem.dataset.pos == 1);
//   const first = elems.find((elem) => elem.dataset.pos == -2);
//   const last = elems.find((elem) => elem.dataset.pos == 2);
  
//   current.classList.remove('carousel__item_active');
  
//   [current, prev, next, first, last].forEach(item => {
//     var itemPos = item.dataset.pos;

//     item.dataset.pos = getPos(itemPos, newActivePos)
//   });
// };

// const getPos = function (current, active) {
//   const diff = current - active;

//   if (Math.abs(current - active) > 2) {
//     return -current
//   }

//   return diff;
// }
   }



